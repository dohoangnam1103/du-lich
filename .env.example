DATABASE_URL=postgres://ddl:ddl_dev_pw@localhost:5433/dia_diem
AUTH_SECRET=replace_me
AUTH_GOOGLE_ID=replace_me
AUTH_GOOGLE_SECRET=replace_me

# Public canonical URL of the deployment (required behind a reverse proxy /
# tunnel so Auth.js trusts the host and builds correct OAuth callback URLs).
AUTH_URL=http://localhost:3000
AUTH_TRUST_HOST=true
